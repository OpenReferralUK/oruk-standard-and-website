# Use the familiar node_modules layout rather than Yarn's PnP resolver, so that
# Next.js, Playwright and ESLint plugin resolution behave exactly as they did
# under Yarn 1. PnP is a larger change and is deliberately not adopted here.
nodeLinker: node-modules
